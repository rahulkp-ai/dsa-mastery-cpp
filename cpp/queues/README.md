# Queues — C++20

## Topics
- Queue with STL
- Priority queue (heap)
- BFS with queue
- Sliding window maximum

## Files
- `queue.cpp` — Main implementations

## Compilation
```bash
g++ -std=c++20 -O2 -o solution queue.cpp
```

## Complexity Note
All implementations include time and space complexity in docstrings.
