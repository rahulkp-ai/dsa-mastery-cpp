# Heaps — C++20

## Topics
- Min/max heap with priority_queue
- Custom comparator
- Top-K elements
- Heap sort

## Files
- `heap.cpp` — Main implementations

## Compilation
```bash
g++ -std=c++20 -O2 -o solution heap.cpp
```

## Complexity Note
All implementations include time and space complexity in docstrings.
