name: dsa-mastery
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - numpy
  - matplotlib
  - networkx
  - jupyter
  - jupyterlab
  - ipywidgets
  - plotly
  - pytest
  - pip:
      - rich
      - tqdm
      - graphviz
